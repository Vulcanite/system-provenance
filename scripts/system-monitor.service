[Unit]
Description=eBPF System Call and Network Flow Monitor
After=network.target auditd.service

[Service]
Type=simple
ExecStart=/usr/bin/system-monitor
Restart=always
RestartSec=5s
# Optional: run as root (required for eBPF)
User=root
Group=root

[Install]
WantedBy=multi-user.target